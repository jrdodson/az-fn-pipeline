package org.azure.fn.premium.model.utils;

public enum Sources {
    BLOB_DOCUMENT,
    BLOB_IMAGE,
    BLOB_AUDIO,
    TWITTER,
    IMAGE_OCR,
    HTTP_BINARY,
    HTTP_ARTICLE,
    HTTP_ARTICLE_TEXT
}
